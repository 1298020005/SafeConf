# 迭代记录

| 时间 | 阶段 | 做法 | 结果 |
|:-----|:-----|:-----|:-----|
| 4–5 月 | 单目录 | `~/.codex` 单账号使用 | 15+ 条本地会话（后备份） |
| 5/22 | 登出/修库 | `state_5.sqlite` 重建 | 面板列表变空，rollout 仍在磁盘 |
| 5/23 早 | 多账号目录 | `codex-account` + `~/.codex-accounts/qq3506725328` | 与 default 备份分离 |
| 5/23 | 代理错乱 | Extension Host 用 **7897**（Windows 注入） | `/wham/tasks/list` **fetch failed**、空云图标 |
| 5/23 | 代理修正 | Remote **17897** + 去掉错误 `NODE_OPTIONS` | API 恢复 |
| 5/23 | 历史合并 v1 | `rsync` rollout + sqlite `INSERT OR IGNORE` 进 350 | 索引 12→27 条 |
| 5/23 | 远程 CLI 路径 | Windows `chatgpt.cliExecutable` 盖远程 | 远程 spawn `C:\...\codex.exe` 失败 |
| 5/23 | 全量备份 | `archives/codex-backups/20260523-213717-full/` | 可 tar 回滚 |
| 5/31 | 加 348 号 | `qq3485725367` 第二目录 + symlink 切换 | 348/350 历史分裂 |
| 5/31–6/1 | 面板超时 | `mcp_request_timeout`、app-server 高 CPU | 与代理/进程僵死相关，杀进程 + Reload |
| 6/1 | 方案讨论 | Cockpit 调研：只换 auth、不合并跨号云历史 | 明确「要合并 = 本地文件+索引」 |
| **6/1** | **统一数据** | `codex-unify-data.sh` → 单 `~/.codex` + `~/.codex-profiles` | 22 rollout、37 threads |
| **6/1** | **一键切号** | `codex-switch 348\|350` | 只换 auth，数据目录不动 |

## 架构演进简图

```text
单 ~/.codex
    → 分目录 ~/.codex-accounts/{350,348} + symlink
        → 统一 ~/.codex + profiles 快照 + codex-switch
```

## 计划文档（设计稿）

`/home/yyf/plans/2026-05-23-codex-cockpit-single-home/`（PLAN / CODEX-REVIEW，未删）
