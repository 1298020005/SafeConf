# 给作者的说明书：额度到期后会发生什么（GLM，2026-08-17 凌晨写给杨一帆）

用最直白的话讲清楚。你不用记任何术语。

## 现在的状态（我离开时）

- 最后一个模型（jurkat 第 4 次训练）正在 GPU1 上跑，今天下午 1 点到 2 点半之间
  跑完。前 15 个全部完成，且**从头到尾没有看过任何"答案"**（这是这次实验最值钱
  的地方，必须保住）。
- 我已经布置了一个"值班机器人"：每 45 分钟醒一次，看训练完了没有；完了就按
  事先冻结好的七步流程一步步做：封存 16 个模型 → 生成预测 → 算风险分 → 存档并
  推送到两个代码仓库 → 最后才打开"答案" → 打分。每一步它都会先检查上一步的
  产物没问题才继续；任何一步不对，它会停下来并把原因写清楚，不会硬来。
- 它绝不会做的事：提前看答案、改数字、删坏结果、动你的另一块 GPU。

## 你醒来后看三个文件（都在 proj/agents/glm/ 下）

1. `STATE.md` —— 一眼看到现在走到第几步、每步几点、检查结果。
2. `05_E201_RESULTS.md` —— 如果出现了这个文件，说明结果已经出来（预计明天，
   8 月 18 日）。里面是四个细胞系各自的打分和三道"及格线"的判定，好坏都会写。
3. `03_THINKING_LOG.md` —— 我的全过程想法记录（这个主要是给 AI 看的，你想了解
   推理过程也可以读）。

## 如果 STATE.md 里写着 BLOCKED（卡住了）

不要慌，也不要自己改实验。卡住的原因会写在同一行。最常见的是网络推送失败
（重推一次通常就好）或磁盘/显卡问题。把那一段截图发给任何一个 AI 助手
（包括下次续费后的我），说"按 agents/glm/04_E201_RUNBOOK.md 从 BLOCKED 的那步
继续"，任何人都能接着做——所有命令我都写死在那份手册里了，照抄即可。

## 如果值班机器人根本没跑起来

也没关系。你（或任何 AI）手动照 `04_E201_RUNBOOK.md` 做就行：里面每一步的
完整命令都是复制粘贴级的。顺序千万不能倒：**必须先把预测和风险分推送到两个
远程仓库，才能打开答案**——这是实验的公信力所在。

## 跟周老师汇报怎么说（不提分区、不提统计量）

> 公开模型负责预测"关掉某个基因后其他基因怎么变"；我的工作是在预测发出去、
> 真实验还没做的时候，提前标出哪些预测可能错、值得优先检查。四种细胞（K562、
> RPE1、HepG2、Jurkat）轮流整组留出，每种独立训练四次，先保存预测和风险排序、
> 推送到代码仓库，然后才打开真实实验数据打分。目前结论是：这个"风险信号"不是
> 什么时候都好用——在没见过的细胞里，反而是"预测变化越大越容易错"这条最简单的
> 规则最灵。所以系统里加了一个开关：信号没被验证过的场景，就明说"现在不能排序"，
> 而不是硬给一个排名。

## 需要你本人做的三件小事（不急，投稿前做完）

1. **打开一次 ConfPert 的 OpenReview 网页**
   （https://openreview.net/forum?id=1uE9rtYYzp），用浏览器把全文看完，确认它
   没有"弃用/停用"实验——这是离你最近的竞品论文，服务器上的自动工具被验证墙
   挡住了，只能人看。看的时候问自己：它有没有说"什么情况下不给覆盖"？
2. **问学院一句话**：毕业认哪一年的期刊分区表、认大类还是小类？
   （中科院分区表以后不再更新了，最后一版是 2025 年的。）
3. **确认作者名单**：谁通讯、基金号填什么。论文里留了空。

## 论文和图在哪

`proj/agents/glm/paper/`：英文正文、中文正文（两个都完整可读，关键实验的数字
留了明确的"待填"标记，结果出来后照 `paper/README.md` 的清单替换）、五张图
（四张主图+一张补充图，300dpi 白底）、两封投稿信（主投 Briefings in
Bioinformatics，备投 Bioinformatics）。

## 一句话总结

**训练照跑、机器人值班、结果明天出、命令全写死、论文框架已就位。**
你今晚可以睡觉。
