# ===========================================
# SafeConf .gitignore - 三道安检门
# ===========================================

# === 第一道门：大文件类型一律不要 ===
*.npz
*.h5
*.hdf5
*.pkl
*.pickle
*.pt
*.pth
*.ckpt

# 2026-07 E162 曾因上游 Lightning 的 hparams 序列化缺陷，把完整模型错误写进
# 139 MB 的 YAML。原文件留在服务器供失败审计；GitHub 单文件上限为 100 MB，不能
# 将它纳入普通 Git。具体 SHA-256 与恢复边界见 docs/项目交接_20260812.md。
/docs/实验结果/E162_wessels_prescribe_native_20260714/attempt_001/seed_3407/logs/main/version_0/hparams.yaml

# === 第二道门：这些文件夹全是产物/数据 ===

# 实验输出（几百M到几G）
**/outputs/
**/_PACKAGES/
**/confidence_task/outputs/

# 原始模型与预测缓存（保留在服务器或对象存储，不进入 Git）
*.npy
*.npy.gz
*.h5ad
*.zarr
**/arrays/
**/raw_*/
**/checkpoints/
**/logs/
**/models/
*.csv.gz

# Python 缓存
**/__pycache__/
*.pyc
*.pyo

# Grok /copy 自动缓冲（每次对话更新，不入库）
agents/grok/.copy_buffer.txt
agents/grok/COPY.txt
agents/grok/COPY.gbk
agents/grok/COPY.html
agents/grok/.copy_server.pid
agents/grok/.copy_server.log

# 打包产物
code/_ARCHIVE/

# 汇总展示区（含 zip 和 symlink）
show/

# 本地运行产物全部留在服务器，不进入 Git
runtime/

# 历史实验目录中的 CSV 已按大小清理。现有轻量汇总表仍受 Git 跟踪；
# 新表需要人工确认后用 `git add -f` 加入，避免任务级明细再次灌入仓库。
docs/实验结果/**/*.csv
submission/**/*.csv
code/**/*.csv

# codex 输出 zip
output/

# 项目内不再新建分散 `_archive`。历史材料统一放到
# `/home/yyf/archive/safeconf/`，因此不在此忽略，让误建目录立即出现在 Git status。

# Qoder 本地缓存
.qoder/

# === 第三道门：杂杂 ===

# zip 包不进 git（太大）
*.zip
*.tar.gz
*.rar
*.docx
*.pptx
*.xlsx

# Jupyter 检查点
.ipynb_checkpoints/

# 系统垃圾
.DS_Store
Thumbs.db
