# 2026-09-17 实验实时状态与自动接力

复核时间：2026-09-17 12:25（Asia/Shanghai）

## 当前结果

- E205 Exphormer 跨结构实验完成 11/16；GPU 0 正在运行 `hepg2/seed_3`，
  GPU 1 正在运行 `jurkat/seed_3`，队列中还剩 3 个未启动任务。
- 两张 Quadro RTX 6000 均保持约 82%–83% GPU 利用率，各占 14–15 GB 显存；
  单卡再叠加一个同规模训练会超过安全显存，不增加第三个进程。
- 11 个已完成模型的 checkpoint 已提前完成独立体检：文件哈希、80 轮计数、模型
  结构、数据视图、参数有限性和零目标真值访问全部通过，模型参数结构哈希一致；
  失败数为 0。
- E205 目标真值仍是 `NOT_AUTHORIZED`。当前看到的验证集损失不用于选择模型、公式
  或任务，16 个模型完成并封存前不读取正式误差。

部分 checkpoint 体检记录：

```text
/home/yyf/data/txpert_official_20260802/e205/formal/
  _partial_audit/E205_PARTIAL_CHECKPOINT_AUDIT.json
```

## 已经启动的无人值守链

```text
E205 剩余 5 个模型训练
  -> 16/16 checkpoint 重新总体验证并计算 SHA-256
  -> 两张 GPU 并行生成 16 份预测
  -> 封存预测、风险特征和注册证书，推送 GitHub/Gitee
  -> 单独授权正式真值，一次性运行 5,000 次簇自助评价
  -> 自动生成 REPORT.md 和 E205_RESULT_OVERVIEW.svg
  -> Jiang24 H5 冒烟测试
  -> 自动启动 E208：LatentAdditive seeds 1–4 + LinearAdditive seed 1
```

E208 正式队列已经在 `safeconf_e208_formal_queue` 会话中等待。它只继承冒烟测试通过
的预登记 batch size，不根据结果改模型或删任务；五个模型继续保持 `test=false`，
预测和目标真值评价仍是后续两个独立阶段。队列状态：

```text
/home/yyf/data/perturbench_e208/formal_20260917/
  E208_FORMAL_QUEUE_STATUS.json
```

服务器重启后，用户级 `crontab @reboot` 会恢复 E205 训练、E205 训练后结果链和 E208
正式训练等待队列。重复执行会先检查现有 tmux 与 checkpoint，不会另开一套覆盖结果。

## 时间窗口

- `hepg2/seed_3` 当前约第 57/80 轮；`jurkat/seed_3` 当前约第 7/80 轮。
- 按同目标已完成任务的实际吞吐，E205 训练仍预计在 **9 月 19 日下午至晚间**完成。
- checkpoint 封存、16 份预测、双远端存档和正式评价预计再需 **12–24 小时**，
  第一份完整 E205 结果仍预计在 **9 月 20–21 日**形成。
- E208 冒烟通过后会从日志测得单轮耗时，再写入五个模型的正式完成时间；现在不写
  没有实测依据的日期。

## 结果不理想时的处理边界

图表或指标未达到预登记门时，允许继续做三类工作：修正能够复现的程序错误并保留旧
结果；按已经冻结的强基线、跨结构和外部数据合同补证据；把方法主张收窄到实验实际
支持的范围。不会改真实误差、删除失败细胞系、事后挑随机种子或换指标制造正结果。
这样本轮最终至少会留下可复算的跨结构结论、明确的失败边界和 Jiang24 外部验证，
而不是只保留一张好看的图。

本次自动化与测试提交：`de23884`。GitHub、Gitee 均已同步；63/63 项测试通过。
