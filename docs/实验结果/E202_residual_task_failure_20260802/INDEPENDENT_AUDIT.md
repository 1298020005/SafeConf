# E202 独立验收

- 发布任务：566 个 `primary_ge30`，任务键唯一；
- 发布端点记录：2,830 个 GAT–general baseline 配对，即 566 × 5；
- `E202_OUTPUT_HASHES.csv` 登记的 12 个文件全部重新计算 SHA-256，零差异；
- 不调用 E202 的统计函数，直接对源列作平均秩、带截距残差化和相关计算，得到主
  partial Spearman `-0.06795009555675122`；
- 正式状态中的点估计为 `-0.06795009555675122`，两者完全一致；
- Attempt 3 的八张统计表和正式报告与保留的 Attempt 2 逐字节一致；
- PNG 原始分辨率为 2,460 × 1,950，白底，四个面板及标题均未截断。

验收结论：`PASS`。主统计区间为 `[-0.1522093818, 0.0190646297]`，因此冻结门槛
给出的科学结论是 `NOT_SUPPORTED`。这表示在 K562 的当前 TxPert-GAT 审计中，
训练背景分歧尚不能在控制预测幅度后识别 GAT 相对 general baseline 的额外 RMSE。
