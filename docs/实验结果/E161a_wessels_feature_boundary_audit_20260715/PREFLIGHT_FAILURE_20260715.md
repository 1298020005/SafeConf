# E161a 首次启动前置失败

时间：2026-07-15

首次调用在任何 `X` 索引之前停止。原因是源码中的单一访问点自检使用了完整字符串字面量，既匹配真正的索引语句，也匹配自检语句自身，错误报告为两个访问点。

修订仅将自检字符串拆分拼接，使其不再计数自身；数据范围、候选边界、统计量和判断规则均未改变。失败时尚未生成结果文件，validation、test、excluded 表达访问仍为 0。
