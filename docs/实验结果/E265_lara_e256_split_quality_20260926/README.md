# E265｜Lara ex vivo 原始效应重建尝试

本轮尝试从官方 h5ad 重新重建 Lara ex vivo 的 E256 固定 5,000 基因、最少 6 个细胞、每对最多 2,200 个细胞、随机种子 5201 任务，并复算来源内 split-half 测量质量。

## 状态

原始效应向量抽查可以对齐，但所有来源效应方差与已归档 E256 表的最大绝对差为 `0.1425170898437358`，因此在没有查清浮点归约、细胞截取顺序和原始预处理差异之前，**没有生成正式结果，也没有把该重建用于方法比较**。脚本保留在 `tools/scripts/run_e265_lara_e256_split_quality.py`，失败状态见 `STATUS.json`。

这条记录的作用是防止把“看起来相同的 h5ad”误当成与历史结果完全相同的输入。后续如需补 Lara 的 split-half，必须先解决这一合同差异。
