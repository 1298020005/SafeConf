# E263｜Tahoe 预真值表达抽取

E263 完成了固定条形码集合的表达矩阵抽取：265,984 个选定细胞全部抽取成功，其中训练/验证处理细胞 230,400 个、可用对照细胞 35,584 个；处理测试条目导出数为 0。结果文件位于服务器数据盘，Git 只保存状态、哈希和边界，不提交约 1.2 GB 表达文件。

这一步是数据准备，不是 SafeConf 的预测或最终验证。部分无关细胞可能随同一 Parquet row group 被解压，但没有被持久化、聚合或评估为测试处理真值；完整边界见 `STATUS.json`。
