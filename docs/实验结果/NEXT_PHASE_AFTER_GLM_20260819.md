# 对 GLM 工作的独立核查，以及下一阶段实验

核查时间：2026-08-19 01:40（Asia/Shanghai）
核查人：Grok（本会话），对照 `agents/glm/`、ZCode 日志、E201 现场、冻结 CSV

## 1. 你和 GLM 实际要求它做什么

ZCode 会话 `sess_e9562987-…`（8 月 17 日为主，19 日续上）。GLM 自己记下的授权是：

1. 按交接包第 8 节先交审核；
2. 一步步做实验，尤其是 E201 评估链，不许无人核验批处理；
3. 用现有内容写 Nature 格式中英文稿，图要做好；
4. 全部写进 `proj/agents/glm/`；
5. 额度到期后也要能接上。

你后来还要过：科学问题到底是什么、怎么改、怎么解决；分区按导师“尽量高”处理。

## 2. GLM 做对了什么（我核对过）

这些我同意，并且用冻结表对过数字：

- **分区判断**：现在不能投中科院大类一区；Briefings 是现实主投（大类 2 区、小类/JCR 才像“一区”）；“随便二区”不成立。这和 8 月 15 日的审核一致。
- **题目**：`fail-closed post-prediction reliability contract` 与老师三问、已有正负结果对齐。
- **E199/E200 头条数字**：独立复算 ρ=0.3948 / 0.0955 / 0.4240 / 0.8797 与冻结表一致。增量也一致：E199 Δρ=+0.299（CI 全正），Δ效用 CI 跨 0；E200 Δρ=−0.456，Δ效用 −0.548。
- **抓到我交接包里的标签错误**：双未见 −0.349～−0.241 是 E189 的 Spearman，不是 utility。E191 的 20% 效用是分歧 −0.127、幅度 −0.080。这个纠正是对的。
- **证书门是恒等式**：只能当完整性检查，不能当科学发现。
- **真正的科学故事是翻转**：K562 内未见基因上分歧有用、幅度失效；整背景留出上幅度压倒分歧。固定加权两边都赢不了。这不是尴尬，是文章该写的东西。
- **文献补漏**：ConfPert、GEARS 湿实验优先级、Molina 同款四细胞面板、PerturBench 与 scPerturBench 不要混称。ConfPert 全文它没打开，留给你人工看，这点诚实。
- **盲态守住了**：训练 16/16 完成，target 真值仍未开。8 月 19 日 01:34 封存了 16 个 checkpoint，提交 `cd1779e`，GitHub/Gitee 已同步。

## 3. GLM 哪里不够、或说重了

1. **值班自动化没跑起来。** 它自己写了 45 分钟 cron，`runCount=0`，额度一停就没了。`07` 说明书里把“醒来看结果”说得太满。实际：训练 17 日下午就完了，封存拖到 19 日凌晨才做。
2. **`STATE.md` 自相矛盾且过时。** 正文写 STAGE_1 执行中，表里 STAGE_0 还是进行中、STAGE_1 未开始。现场已经是 **STAGE_2 完成**（seal 已双远程）。
3. **图 7 条件合同模拟是事后示意。** 足迹标签来自同一批已解盲数据。它在批判文档里写了，英文稿仍把模拟写得很像方法证据。投稿时必须压成“illustrative / in-sample”，前瞻只认 E201。
4. **E201 的“家族分歧”是四种子、同一 STRING-GAT**，不是 scGPT–GEARS 那种跨架构分歧。正文必须始终写清，否则审稿人会说你们换了对象。
5. **E203（混合家族 + 源侧选权重）现在不要开。** 想法对，但必须等 E201 正式评价结束、老师点头后再冻结。现在开等于用未完成的主实验给自己加戏。
6. **论文稿和审计包还在未跟踪目录。** `agents/glm/`、`code/safeconf_audit/` 没有进 Git。评估链脚本只检查 seal/脚本是否已提交，不拦这些文件；但下一台电脑拉不到 GLM 的稿。
7. **软件包 README 待办和“已做成”打架。** `paper/README.md` 仍把 pip 包列为待办，`STATE.md` 说 13 项全过。以 `code/safeconf_audit/` 现场为准，投稿前要统一说法。

总评：审核和主张收窄是合格的；科学批判里“固定分数必然顾此失彼”也成立。它没有毁掉盲测，也没有编造 E201 结果。它没完成的是评估链本身——而这才是现在唯一该做的实验。

## 4. 现场进度（2026-08-19 01:36）

| 项 | 状态 |
|---|---|
| E201 训练 | 16/16 COMPLETE，jurkat seed 4 于 08-17 13:04 结束 |
| 训练服务 | 已停；GPU1 空闲；GPU0 仍是系统 VLLM |
| family seal | `SEALED_16_CHECKPOINTS`，16 行，`target_truth_opened=false` |
| Git | `cd1779e`，本地=Gitee=GitHub |
| 零真值预测 | **尚未开始**（没有 `predictions.npy`） |
| 真值 | 仍未释放 |

## 5. 下一阶段实验（只这一条主链）

**现在到出数：只走 E201 评估链。不新开 E203，不重跑 E198–E200，不打开真值。**

```text
STAGE_3  16 份零真值预测（每细胞先 seed 1，再 2–4）  ← 此刻开始
STAGE_4  风险特征 + official general baseline + E200 等价性
STAGE_5  预测前产物双远程提交
STAGE_6  才释放 target 真值
STAGE_7  三门分判，四个细胞全报
STAGE_8  把数字填进 GLM 文稿的占位符
```

命令以 `agents/glm/04_E201_RUNBOOK.md` 为准。GPU 只用 GPU1。  
预测目录已存在则脚本会拒绝覆盖，所以每步核验后再进下一步。

E201 出数后再分叉（与 GLM 三叉一致，我同意）：

- 多数细胞过路由门 → 按 Briefings 收正文；
- 部分过 → 异质性就是文章；
- 全不过或幅度全面主导 → 停“优于幅度”，写停用规则。

**此时仍不要做：** E202b 新数据 PRESCRIBE、湿实验、改 E201 公式、把 chemical 塞进主表。

## 6. 本回合已执行

- 核验 16/16、seal、双远程、真值未开；
- 写下本判断；
- 启动 STAGE_3：`CUDA_VISIBLE_DEVICES=1`，K562 seed 1 零真值预测（batch 16）。
