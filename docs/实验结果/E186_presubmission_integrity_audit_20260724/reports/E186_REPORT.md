# E186｜SafeConf 投稿前完整性对抗审计

## 最终裁决

当前计算证据包通过投稿前完整性审计：

```text
18 checks passed
0 checks failed
audited release head = d52f3dd
```

这里的 `PASS` 表示证据链、报告口径、复现和 Git 留存一致，可以进入强二区目标的成稿与投稿流程。它不改写任何单项实验结果。

## 1. E182 解封顺序

Git 祖先关系和提交时间均满足：

```text
b4701ac  冻结元数据与任务
    ↓
fc0de74  固化 Python 3.9 与来源链
    ↓
9124431 / 2f4f148  封存十模型 pretruth 预测
    ↓
420e536  打开校准并冻结 conformal 上界
    ↓
593f663  一次性打开最终评价真值
    ↓
744b5ef  真值解封后的四研究描述性合并
```

四项 `git merge-base --is-ancestor` 检查均返回 0。校准发生在 pretruth 预测封存之后，最终评价发生在校准冻结之后，E183 明确晚于 E182。

## 2. 数字与单位

E185 在 Python 3.12.4 和 Python 3.9.25 上都重算通过：

- 4 项研究；
- 2,433 个 guide/context 评价任务；
- 737 个 `(study, target_cluster)` 靶点簇；
- 家族 RMS 与最坏成员下界违反均为 0；
- 家族上界任务覆盖 2,331/2,433；
- 家族上界靶点簇同时覆盖 666/737；
- 最坏成员上界靶点簇同时覆盖 688/737。

报告先分别给四项研究，再给 E183 描述性合并。任务覆盖和靶点簇同时覆盖没有混成同一个统计量。

## 3. 失败边界

最高 gate、Agent 入口、GPT 入口、项目 README、E182、E183、E184 和 E185 均保留以下事实：

```text
E182 family target coverage = 16/20
registered requirement = at least 17/20
E182 status = FAIL
```

`P(K≤16)=18.68%` 只解释有限校准波动。没有用它把 E182 改判为通过，也没有事后更换阈值、模型成员或删除失败靶点。

## 4. 理论与竞品归属

E184 已把三类成分分开：

1. 平方误差 ambiguity decomposition 属于经典集成理论；
2. `Δ/2` 来自三角不等式；
3. split conformal 的有限样本覆盖属于成熟统计方法。

SafeConf 的可写贡献限定为注册家族证书对象、参考质心上界搬移、靶点簇校准、fail-closed 真值访问和四项单细胞研究验证。没有把 GEARS uncertainty、PRESCRIBE 或 GPerturb 误写成同一输出，也没有声称全面性能胜出。

## 5. 复现与远端

- E181 的 19 个 manifest 条目全部通过；
- E183 的 6 个直接输入哈希全部通过；
- E185 连续两次生成的摘要报告和主数字 CSV 哈希一致；
- E185 在 Python 3.9.25 和 3.12.4 上均为 12,033 项检查、0 失败；
- 审计开始时，本地、GitHub 与 Gitee 分支头均为 `d52f3dd4f8524af4ee2f6f64eb0472845f436aba`。

E186 提交后还需再次核对两个远端指向 E186 提交；该结果由提交日志和最终交接信息记录。

## 6. 当前可执行判断

服务器可控部分已经具备强二区目标所需的计算方法证据主干：

- 明确、可复核的输出对象；
- 经典理论来源和新增组合边界清楚；
- 多研究、预注册、负结果与失败停止规则齐全；
- 一条命令复现主数字；
- 双远端版本留存。

后续工作不再通过已开真值数据改公式。用户此前明确不让本轮代写论文，因此本轮停在“证据包与投稿基础设施完成”，不越权生成正文。成稿时必须按 E184 的贡献边界和 E182 的失败口径书写。
