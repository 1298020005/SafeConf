# E230：固定预测下的历史参考库容量与来源多样性

2026-09-22，执行前固定并提交。E228/E229的问题先记录，本轮不训练跨目标误差评分器，不使用其可能交叉接触目标表达的训练表。

## 输入与隔离

E201每个目标的源数据只读`E201_blind_<target>/de_adata_test.h5ad`。逐一验证原哈希及目标扰动行数为0；只读来源扰动细胞和匹配批次的来源对照。保留原任务顺序、上游家族预测和目标误差，不重训上游。目标真实误差仅由评价阶段使用；这是已公开标签上的机制开发分析，不是新盲测。

## 固定设计

- 4个目标全部保留；主要任务沿用E201的1808个primary_ge30，2008全任务为敏感性。
- 每个目标3个可用来源背景，以种子11/23/47的SHA排序确定嵌套前1/2/3个背景；不能按误差挑来源。
- 数量网格：每个可用来源/扰动真实细胞抽样25%/50%/100%，置换固定且样本嵌套，最少1细胞。匹配对照保持固定，仅改变扰动细胞。
- 多样性匹配网格：固定总计30个来源扰动细胞，来自1/2/3背景时各采30/15/10细胞。预先以元数据选出三个背景各至少30细胞的同一组条件；未满足的任务不参与此子分析，但保留在上面的全任务分析，数量与ID公开。此处只证明该覆盖子群的多样性作用。
- 没有来源支持时回退到幅度并报告覆盖；不删除零支持任务。单来源离散度缺失，按该配置的已观察来源离散度中位数填补；全缺失时置0并保留缺失标记，不能读成确定低风险。
- 每个任务重算来源均值、预测—来源RMSE、跨来源离散、实际细胞量、覆盖缺口。跨来源均值按背景等权。

## 固定评分与对照

固定规则沿用五项风险S：D、预测—来源差异、来源离散、−log(1+细胞量)、背景缺口，按目标配置的primary任务标准化后等权平均；常数项标准化为0。

主评分`0.8·rank(M)+0.2·rank(S)`，rank为同目标批次百分位；零来源时用rank(M)。比例固定，不搜索。公开原始M、D、S、固定M+D和主评分全部结果。S只作历史机制对照，不自动覆盖E201旧分数。

主要容量比较：3来源/100%相对3来源/25%的20%复核效用，以及主评分相对幅度；多样性比较：匹配总30细胞的3来源相对1来源。先平均抽样种子，再目标等权；区间为4目标描述性重采样。10%/30%预算和相关性为辅助。

预定内部门（不等于投稿门）：两项主要容量/多样性差值均>0、各至少3/4目标正向，且全容量主评分相对幅度至少+0.02、描述性区间下限>0。任一不满足记NOT_SUPPORTED；不得从27配置里挑最优替代主结果。

## 审计和产物

最大容量的来源特征需复算回E201冻结特征（数值容差1e-5）；模型预测误差和幅度在所有配置必须不变。保存每任务无误差特征/分数、来源读取和元数据清单、文件哈希、完整指标。E208测试真值读取0；不改其队列。

CPU最多2个进程、每进程数值线程1；原始表达数据和向量不入Git，压缩特征及摘要入Git。更多细胞和更多研究是否真正有用由本轮决定，不预设正向结果。
