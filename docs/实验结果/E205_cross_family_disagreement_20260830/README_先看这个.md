# E205 先看这个

E205 不是当前 E201 的重复统计，也还没有科学结果。它专门检查“四个随机种子”是否被误解成“多个不同模型”。E201 的模型身份是 TxPert-STRING-GAT；E205 新增 TxPert-Exphormer，并保持整细胞留出的数据与训练合同不变。

2026-09-10 已完成运行方案和训练适配器，下一步先跑 `RPE1 × seed 1 × 1 epoch` 的工程 profile。profile 只验证显存、运行时间、checkpoint 和零目标真值访问，不作为结果。通过后才扩成四目标、四种子的 16 个正式模型。

详细安排见 `EXECUTION_PLAN_20260910.md`。
