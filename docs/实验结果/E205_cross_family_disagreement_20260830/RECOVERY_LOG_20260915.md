# E205 服务器重启与恢复记录

记录时间：2026-09-15

## 事件

- 服务器于 2026-09-15 12:31（Asia/Shanghai）重启；
- 原 E205 队列、HepG2/seed2 与 RPE1/seed4 训练进程随系统中断；
- 日志未显示模型、数据或 CUDA 主动报错；
- 中断前 checkpoint 保留：HepG2/seed2 约第 57 轮，RPE1/seed4 约第 70 轮；
- 目标扰动真值仍未授权读取。

## 恢复过程

13:24 后从各自 run directory 的 `checkpoints/last.ckpt` 恢复。首次同时启动手工恢复与
双卡监管器时，GPU1 出现启动竞态：监管器尚未观察到 RPE1 已占用显存，短暂启动了
Jurkat/seed2。该 Jurkat 进程在训练早期被终止，目录按基础设施中断归档，不作为科学
失败或模型失败。

随后停止原监管器，保留已经恢复的两个训练子进程；确认两卡各只有一个正式训练后，
重启恢复监管器。恢复监管器把两个进程识别为 detached job，并因显存占用阻止新任务
进入对应 GPU。

两个 Jurkat 队列日志改名为 `*.infra_recovery_20260915.log`，完整保留命令与退出码，
但不再匹配队列的科学重试计数规则。恢复监管器重新载入后，Jurkat/seed2 的可用重试
次数没有被这次基础设施竞态消耗。

## 13:31 合规状态

| GPU | 任务 | PID | 状态 |
|---|---|---:|---|
| 0 | HepG2/seed2 | 151608 | 从断点续训 |
| 1 | RPE1/seed4 | 150654 | 从断点续训 |

监管器会在现有任务完成、显存释放并通过完整性检查后继续剩余队列。当前完成 6/16，
等待 8，永久失败 0，目标真值访问状态为 `NOT_AUTHORIZED`。

## 解释边界

- 两次 Jurkat 启动中断源于恢复操作竞态，不进入模型性能统计；
- 不修改训练数据、超参数、轮数、评价指标或目标真值；
- 科学结果仍需 16 个 checkpoint 完成、盲预测与风险/证书表封存、双远程留痕后才能
  一次性评价；
- 如果服务器再次重启，只允许从同一 run directory 的已审计 checkpoint 恢复。
