# E205 断点恢复记录

记录时间：2026-09-12

## 发生了什么

E205 的一轮工程 profile 已于 2026-09-10 21:59 完成：

- target：RPE1；
- seed：1；
- 一轮训练耗时：987.61 秒；
- 峰值保留显存：15,183,380,480 bytes；
- `last.ckpt` 已写出；
- 目标扰动表达读取数为 0；
- 正式扩展门通过。

正式队列随后在 GPU 0、1 分别启动 K562/seed 1 和 RPE1/seed 1。服务器于 2026-09-11 14:06 重启，两个训练进程和队列监管器同时中断。日志中没有 Python、CUDA 或模型异常；系统启动记录明确显示重启。

## 已保留的断点

| 任务 | `last.ckpt` 保存轮次 | global step | 中断前日志进度 |
|---|---:|---:|---:|
| K562 / seed 1 | 53 | 248,832 | 第 54 轮 |
| RPE1 / seed 1 | 57 | 247,370 | 第 58 轮 |

两个 checkpoint 均非空，模型参数、优化器、学习率调度器和 Lightning 循环状态齐全。只丢失尚未落盘的半轮以内进度，不需要从第 1 轮重训。

## 恢复改动

- Exphormer 适配器新增 `--resume-checkpoint`；
- 只接受当前 run directory 内的 `checkpoints/last.ckpt`；
- 恢复前核对 experiment、kind、target、seed 和模型家族；
- 恢复历史记录 checkpoint 的路径、字节数、SHA-256、旧提交和新提交；
- 正式队列遇到无活动进程但存在合格断点的 `RUNNING/FAILED` 项时，进入续训，不再把它误归档后从头开始；
- 无合格断点的失败目录仍整体归档，最多尝试两次。

## 结果边界

目前只有工程 profile 通过。两个正式模型没有完成到第 80 轮，也没有生成目标预测，因此没有 E205 风险排序结果，不能据此判断 SafeConf-M 是否优于预测幅度。

