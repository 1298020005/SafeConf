# E208仅调整调度，不调整科学协议

> 2026-09-23复核：该备用调度器没有被启动。检查时原有正式队列已经完成五个模型训练，并在验证集能力门处停止；测试扰动表达仍读取0行。脚本仅保留为以后“训练完成一个便验证一个”的可复用工具，不构成本轮运行链的一部分。

2026-09-22。原后训练队列等待五个训练任务全部结束，GPU0在四个已完成模型尚未验证时空闲。新增ready-validation前置调度器：只对状态COMPLETE、注册架构/种子一致、检查点存在、测试表达读取0的模型运行原验证脚本，输出到原验证目录。

保留训练队列正在使用的GPU，优先用GPU0；不改训练批大小、轮数、早停、模型、验证任务或合格标准，不读取测试表达。所有五个验证完成后exec恢复原后训练队列；原队列识别既有验证PASS产物，再执行原能力门、原封存预测阶段。

替换时只停止确认处于WAITING_FOR_TRAINING_AND_VALIDATION_CACHE且无子进程的旧后训练监管器；不停止训练进程。旧状态快照留在运行目录。新增状态E208_READY_VALIDATION_STATUS.json和日志。若有运行中子任务，不自动替换。
