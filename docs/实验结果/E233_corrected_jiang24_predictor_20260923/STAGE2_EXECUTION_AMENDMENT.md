# E233 补种子执行门

登记时间：2026-09-23，阶段1两个候选仍在训练、Jiang24 测试扰动表达读取0行。本补充只落实原 `ANALYSIS_CONTRACT.md` 中“若验证通过，补种子2–4”的动作，不改变阶段1选型与验证门。

1. 只有 `E233_STAGE1_SUPERVISOR_STATUS.json` 明确写 `STAGE1_PASS` 且有 `selected_variant`，才启动被选变体的随机种子2、3、4。若 `STAGE1_COMPETENCE_BLOCKED` 或任意训练/验证失败，阶段2记 `BLOCKED`，不启动训练。
2. 四个种子使用同一 PerturBench 固定提交、Jiang24固定输入、相同训练和推理控制合同、批大小、优化器、最多400轮、早停耐心50。仅随机种子不同。每个新种子单独日志、checkpoint和哈希，不覆盖阶段1。
3. 两张24GB GPU 最多并行两个训练；第三个等待空位。CPU/HDF5 可能成为瓶颈，不通过无效计算制造 GPU 占用率。
4. 三个新种子完成后，逐个在原216个验证任务上生成预测；连同阶段1被选种子，固定四种子等权平均。四种子平均预测的验证MSE必须低于无变化基线，且12个细胞状态中至少8个不劣于无变化。未通过则不生成测试预测、不读取测试扰动表达。
5. 即便通过，后续仍须按独立预先登记的测试预测、风险分数封存及远程留存流程执行。验证成功不等于外部测试有效。
